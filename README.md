# Docker Image of php-fpm for Grav

## Building the image from Dockerfile

``` bash
docker build -t php_fpm:grav .
```

## Configure php-fpm with your web-server (like nginx or apache):

Expose port 9000
